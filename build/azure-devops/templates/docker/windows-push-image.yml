parameters:
  - name: imageDisplayName
    type: string
    default: ''
  - name: imageName
    type: string
  - name: tags
    type: string

steps:

  - task: Docker@2
    displayName: 'Pushing image ${{ parameters.imageDisplayName }}'
    inputs:
      containerRegistry: 'GitHub Container Registry'
      command: 'push'
      repository: '${{ parameters.imageName }}'
      tags: '${{ parameters.tags }}'
