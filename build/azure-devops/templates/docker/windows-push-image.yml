parameters:
  - name: imageDisplayName
    type: string
    default: ''
  - name: imageName
    type: string

steps:
  - task: Docker@2
    displayName: 'Pushing image ${{ parameters.imageName }}'
    inputs:
      command: 'push'
      repository: '${{ parameters.imageName }}'